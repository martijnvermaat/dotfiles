set hostname=vermaat.name

set use_from=yes

set from="martijn@vermaat.name"

set realname="Martijn Vermaat"

set delete=ask-yes

#set spoolfile="~/Maildir/"

#set mbox="~/Mail/Archive"

set move=no

#set signature="~/.sig"

set record="=.Sent"

set mbox_type=Maildir

#set folder="~/Maildir/"

#mailboxes !
#mailboxes =F-Spot
#mailboxes =Sent
#mailboxes =Archive
#...

save-hook .* =.Archive

set print=ask-no

set pager_stop
bind pager <up> previous-line
bind pager <down> next-line

macro index i "c!\n" "Go to inbox"
macro pager i "c!\n" "Go to inbox"

macro index y "c?" "show incoming mailboxes list"
macro pager y "c?" "show incoming mailboxes list"
